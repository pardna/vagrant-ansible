angular.module('Pardna')
		.controller('AccountPayoutCtrl', ['$scope', '$window', '$state', '$mdToast', 'userService', 'localStorageService', 'jwtHelper', 'userService', 'groupService', 'paymentService', AccountPayoutCtrl]);


function AccountPayoutCtrl($scope, $window, $state, $mdToast, userService, localStorageService, jwtHelper, userService, groupService, paymentService) {
	
}
